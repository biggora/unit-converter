# Changesets

This directory tracks pending releases. Run `pnpm exec changeset` to create a
new changeset (markdown file describing the change + bump kind), then commit it
with your PR. Releases are published by `.github/workflows/release.yml`.

See [the changesets docs](https://github.com/changesets/changesets) for the
full workflow.
